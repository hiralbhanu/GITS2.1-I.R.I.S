language: 
  python
python:
- "3.8"
install:
- pip install -U pip
- pip install -r requirements.txt
- pip install coverage
- pip install codecov
script:
- cd test
- pytest
- coverage run -m pytest -q test/
